INCLUDE_DIRECTORIES(.
  ${GLIB2_MAIN_INCLUDE_DIR}
  ${GLIB2_INTERNAL_INCLUDE_DIR}
  ${GLIBMM2_MAIN_INCLUDE_DIR}
  ${GLIBMM2_INTERNAL_INCLUDE_DIR}
  ${GTHREAD2_MAIN_INCLUDE_DIR}
  ${SIGC++_INCLUDE_DIR}
)

LINK_DIRECTORIES(
  ${CMAKE_LIBRARY_OUTPUT_DIRECTORY}
)

PIXHAWK_EXECUTABLE(mavconn-cameracalibration-auto
  Calibration.cc
  Camera.cc
  Chessboard.cc
  px_autocalibration.cc
  gpl.cc
)
PIXHAWK_LINK_LIBRARIES(mavconn-cameracalibration-auto
  ${OPENCV_CALIB3D_LIBRARY}
  ${OPENCV_CORE_LIBRARY}
  ${OPENCV_IMGPROC_LIBRARY}
  ${OPENCV_HIGHGUI_LIBRARY}
  ${Boost_PROGRAM_OPTIONS_LIBRARY}
  ${Boost_FILESYSTEM_LIBRARY}
  ${GLIB2_LIBRARY}
  ${GTHREAD2_LIBRARY}
  ${GLIBMM2_LIBRARY}
  ${SIGC++_LIBRARY}
  mavconn_shm
  mavconn_lcm
  lcm
)

SET_SOURCE_FILES(CAMERA_CALIBRATION_SRC_FILES
  PxCameraCalibration.cc
  PxCameraCalibrationStandard.cc
  PxCameraCalibrationOmnidirectional.cc
  PxCameraStereoCalibration.cc
)

PIXHAWK_LIBRARY(cameracalibration SHARED ${CAMERA_CALIBRATION_SRC_FILES})
PIXHAWK_LINK_LIBRARIES(cameracalibration 
  ${OPENCV_CORE_LIBRARY}
  ${OPENCV_CALIB3D_LIBRARY}
  ${OPENCV_IMGPROC_LIBRARY}
)
