
PIXHAWK_EXECUTABLE(px_videostreamer px_videostreamer.cc)
PIXHAWK_LINK_LIBRARIES(px_videostreamer
  mavconn_lcm
  ${Boost_PROGRAM_OPTIONS_LIBRARY}
  ${OPENCV_CORE_LIBRARY_OPTIMIZED}
  ${OPENCV_HIGHGUI_LIBRARY_OPTIMIZED}
  ${Boost_FILESYSTEM_LIBRARY}
  ${Boost_SYSTEM_LIBRARY}
  ${GLIB2_LIBRARY}
  ${GTHREAD2_LIBRARY}
)

PIXHAWK_EXECUTABLE(px_videoprepare px_videoprepare.cc)
PIXHAWK_LINK_LIBRARIES(px_videoprepare
  mavconn_lcm
  ${OPENCV_HIGHGUI_LIBRARY_OPTIMIZED}
)
